name: CI Pipeline
on:
  push:
    branches: [main]
env:
  IMAGE: rbnnvrs/devops-tp:latest
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm test
  build-push:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t $IMAGE .
      - run: docker push $IMAGE
